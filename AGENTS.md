# Agent Notes

## Top bar

Each tool page's `<header>` is for navigation and title only — icon, tool name, short description on the left; an "All Tools" link back to `index.html` on the right (same markup/classes across tool pages).

Don't put action buttons (upload, sample, mode toggles, etc.) in the header. Put them in the toolbar/card below it instead.
